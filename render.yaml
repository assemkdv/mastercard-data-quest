services:
  - type: web
    name: mdq2026-hidden-entrepreneurs
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python dashboard.py
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
